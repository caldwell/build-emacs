#!/bin/sh
set -e

. ./notify
. ./build-emacs-functions

cd `dirname $0`
(cd emacs-cvs-clean && cvs -Q update -Pd)
DIR=emacs-cvs-`date +%F`
cp -rp emacs-cvs-clean $DIR

build_emacs "$DIR" Emacs-`date +%F`
